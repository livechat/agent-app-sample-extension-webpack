# agent-app-sample-extension-webpack

```
npm install
npm start
```

Add sample agent app extension by:

```js
App.collections.Integrations.add({
  id: 'salesforce2',
  url: 'https://your-extension.ulr/'
})
```
